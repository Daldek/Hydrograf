# Srodowisko deweloperskie (.venv + docker compose up -d db)

# Database (uzywane przez .venv i docker compose)
POSTGRES_DB=hydro_db
POSTGRES_USER=hydro_user
POSTGRES_PASSWORD=hydro_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# API
LOG_LEVEL=INFO

# IMGW Preprocessing
IMGW_GRID_SPACING_KM=2.0
IMGW_RATE_LIMIT_DELAY_S=0.5
